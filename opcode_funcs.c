#include "monty.h"

void push(stack_t **stack, unsigned int line_number)
{
	(void)(stack);
	(void)(line_number);

	return;
}


void pall(stack_t **stack, unsigned int line_number)
{
	(void)(stack);
	(void)(line_number);

	return;
}
